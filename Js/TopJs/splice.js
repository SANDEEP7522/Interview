const fruits = ['A', 'B', 'C'];

// array.splice('start index', 'remove or add', [element1, element2, ...])

// fruits.splice();
// fruits.splice(1, 1, '0.B'); // replace B with 0.B place of B
fruits.splice(1, 1); // remove B

console.log(fruits);
