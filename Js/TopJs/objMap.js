// Object ka example
const obj = {
  name: "Sandeep",
  age: 20,
  city: "Praygraj",
};

console.log(obj.name);
console.log(obj.age);


// Map ka example
const map = new Map();
map.set("name", "Sandeep");
map.set("age", 20);
map.set("city", "Prayagraj");

console.log(map.get('name'));
console.log(map.get('age'));
